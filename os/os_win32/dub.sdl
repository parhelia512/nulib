name "os_win32"

dependency "numem" version=">=1.1.4"

targetPath "out/"

configuration "library" {
    platforms "windows"
    targetType "staticLibrary"
}

configuration "none" {
    targetType "none"
}
